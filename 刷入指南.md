# 玩客云 WS-1608 ARMBIAN 刷入指南

本文档介绍如何将编译好的 ARMBIAN 镜像刷入到玩客云 WS-1608 设备。

## 📋 准备工作

### 1. 下载镜像文件

1. 访问 GitHub 仓库的 [Actions](https://github.com/jackadam1981/WKY-armbian-build/actions) 页面
2. 找到成功完成的构建运行
3. 在 "Artifacts" 部分下载构建产物
4. 解压下载的 ZIP 文件，找到 `.img` 镜像文件

### 2. 验证镜像文件

下载后建议先验证镜像文件的完整性：

**Windows:**
```powershell
# 如果有 .sha 校验文件，使用 PowerShell 验证
Get-FileHash -Algorithm SHA256 镜像文件名.img
# 与 .sha 文件中的哈希值对比
```

**Linux/Mac:**
```bash
# 验证 SHA256 校验和
sha256sum -c 镜像文件名.img.sha
```

### 3. 准备工具

根据您的操作系统选择相应的刷入工具：

- **Windows**: balenaEtcher、Rufus、USB Burning Tool
- **Linux**: `dd` 命令、balenaEtcher
- **macOS**: `dd` 命令、balenaEtcher

## 🔧 刷入方法

### 方法一：使用 TF/SD 卡刷入（推荐）

这是最简单且安全的方法，不会覆盖原系统，可以随时切换回原系统。

#### Windows 用户

**使用 balenaEtcher（推荐）**

1. 下载并安装 [balenaEtcher](https://www.balena.io/etcher/)
2. 准备一张至少 8GB 的 TF 卡或 SD 卡（建议使用 Class 10 或更高速度）
3. 使用读卡器将 TF/SD 卡连接到电脑
4. 打开 balenaEtcher
5. 点击 "Flash from file"，选择下载的 `.img` 镜像文件
6. 点击 "Select target"，选择您的 TF/SD 卡
7. 点击 "Flash!" 开始刷入
8. 等待刷入完成（通常需要 5-15 分钟）

**使用 Rufus**

1. 下载并安装 [Rufus](https://rufus.ie/)
2. 插入 TF/SD 卡
3. 打开 Rufus
4. 设备：选择您的 TF/SD 卡
5. 引导类型选择：选择 "镜像文件"，点击 "选择" 按钮选择 `.img` 文件
6. 点击 "开始" 进行刷入

#### Linux 用户

```bash
# 1. 查看连接的存储设备
lsblk

# 2. 确认 TF/SD 卡的设备名称（例如 /dev/sdb）
# 注意：请仔细确认设备名称，避免误操作！

# 3. 卸载已挂载的分区（如果有）
sudo umount /dev/sdb*

# 4. 使用 dd 命令刷入镜像
sudo dd if=镜像文件名.img of=/dev/sdb bs=4M status=progress oflag=sync

# 5. 同步缓存
sync
```

#### macOS 用户

```bash
# 1. 查看连接的存储设备
diskutil list

# 2. 确认 TF/SD 卡的设备名称（例如 /dev/disk2）
# 注意：请仔细确认设备名称，避免误操作！

# 3. 卸载磁盘
diskutil unmountDisk /dev/disk2

# 4. 使用 dd 命令刷入镜像
sudo dd if=镜像文件名.img of=/dev/rdisk2 bs=4m status=progress

# 5. 弹出磁盘
diskutil eject /dev/disk2
```

#### 启动设备

1. 将刷好镜像的 TF/SD 卡插入玩客云设备
2. 连接网线到路由器
3. 接通电源启动设备
4. 等待 1-2 分钟让系统启动
5. 通过路由器管理界面或使用 `arp -a` 命令查找设备的 IP 地址
6. 使用 SSH 连接：
   ```bash
   ssh root@设备IP地址
   # 默认密码：1234（首次登录会要求修改密码）
   ```

### 方法二：使用 USB Burning Tool 刷入（高级）

⚠️ **警告**：此方法会直接写入 eMMC，会覆盖原系统，请谨慎操作！

#### 准备工作

1. 下载 [Amlogic USB Burning Tool](https://www.balena.io/etcher/)（Amlogic 官方烧录工具）
2. 准备一条 USB 数据线（USB-A 转 USB-A 或 USB-A 转 Micro-USB）
3. 可能需要短接设备上的特定触点进入烧录模式

#### 操作步骤

1. **进入烧录模式**
   - 断开玩客云电源
   - 找到设备上的短接点（通常在电路板上，可能需要拆机）
   - 短接相应触点（具体位置请参考玩客云拆机教程）
   - 保持短接状态，连接 USB 线到电脑
   - 接通电源
   - 电脑识别到设备后，可以松开短接

2. **使用 USB Burning Tool 刷入**
   - 打开 USB Burning Tool
   - 点击 "文件" → "导入烧录包"，选择 `.img` 镜像文件
   - 确认设备已识别（显示连接状态）
   - 点击 "开始" 按钮开始烧录
   - 等待烧录完成（显示 100%）
   - 点击 "停止"，断开连接

3. **启动设备**
   - 断开 USB 连接
   - 连接网线
   - 接通电源启动
   - 等待系统启动后通过 SSH 连接

### 方法三：通过 U 盘刷入

如果设备已经运行 ARMBIAN 系统，可以通过 U 盘刷入新镜像到 eMMC。

1. 将镜像写入 U 盘（方法同 TF/SD 卡）
2. 将 U 盘插入玩客云
3. 通过 SSH 登录设备
4. 执行以下命令：

```bash
# 查看 U 盘设备
lsblk

# 假设 U 盘为 /dev/sda，eMMC 为 /dev/mmcblk0
# 将 U 盘镜像写入 eMMC
sudo dd if=/dev/sda of=/dev/mmcblk0 bs=4M status=progress

# 同步
sync

# 重启设备
sudo reboot
```

## 🔐 首次登录配置

### 1. SSH 登录

```bash
ssh root@设备IP地址
# 默认密码：1234
```

### 2. 修改 root 密码

首次登录会要求：
- 设置新的 root 密码
- 创建新用户（可选）

### 3. 系统更新

```bash
# 更新软件包列表
apt update

# 升级系统
apt upgrade -y

# 安装常用工具（可选）
apt install -y vim curl wget git
```

## ⚠️ 注意事项

1. **备份数据**：刷入前请备份重要数据，刷入过程会清除所有数据
2. **电源稳定**：刷入过程中请确保电源稳定，避免断电
3. **设备识别**：使用 USB 刷入时，确保设备正确进入烧录模式
4. **镜像验证**：刷入前建议验证镜像文件的完整性
5. **TF/SD 卡速度**：使用 TF/SD 卡时，建议使用 Class 10 或更高速度的卡
6. **首次启动**：首次启动可能需要 1-3 分钟，请耐心等待

## 🐛 常见问题

### Q1: 刷入后无法启动？

- 检查 TF/SD 卡是否完全插入
- 确认镜像文件是否完整（重新下载并验证）
- 尝试使用不同的 TF/SD 卡
- 检查电源是否稳定

### Q2: 找不到设备 IP 地址？

```bash
# Linux/Mac 查找设备
arp -a | grep -i "armbian"

# 或使用 nmap 扫描
nmap -sn 192.168.1.0/24

# Windows PowerShell
arp -a | findstr "armbian"
```

### Q3: SSH 连接被拒绝？

- 确认设备已完全启动（等待 2-3 分钟）
- 检查网络连接
- 确认 SSH 服务已启动：`systemctl status ssh`

### Q4: 忘记 root 密码？

如果使用 TF/SD 卡启动，可以：
1. 将 TF/SD 卡插入电脑
2. 挂载根分区
3. 编辑 `/etc/shadow` 文件重置密码
4. 或重新刷入镜像

### Q5: 如何切换回原系统？

如果使用 TF/SD 卡刷入：
- 只需拔出 TF/SD 卡，设备会从 eMMC 启动原系统

如果刷入了 eMMC：
- 需要重新刷入原厂固件

## 📚 相关资源

- [ARMBIAN 官方文档](https://docs.armbian.com/)
- [ARMBIAN 论坛](https://forum.armbian.com/)
- [玩客云拆机教程](https://www.znds.com/tv-1020120-1-1.html)（搜索相关教程）

## 🔗 相关链接

- [项目仓库](https://github.com/jackadam1981/WKY-armbian-build)
- [ARMBIAN 官方仓库](https://github.com/armbian/build)

---

**提示**：如有问题，请在 GitHub Issues 中反馈。

