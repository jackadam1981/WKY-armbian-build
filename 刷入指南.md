# 玩客云 WS-1608 ARMBIAN 刷入指南

本文档介绍如何将编译好的 ARMBIAN 镜像刷入到玩客云 WS-1608 设备。

## 📋 准备工作

### 1. 下载镜像文件

1. 访问 GitHub 仓库的 [Actions](https://github.com/jackadam1981/WKY-armbian-build/actions) 页面
2. 找到成功完成的构建运行
3. 在 "Artifacts" 部分下载构建产物
4. 解压下载的 ZIP 文件，找到 `.img` 镜像文件

### 2. 验证镜像文件

下载后建议先验证镜像文件的完整性：

**Windows:**
```powershell
# 如果有 .sha 校验文件，使用 PowerShell 验证
Get-FileHash -Algorithm SHA256 镜像文件名.img
# 与 .sha 文件中的哈希值对比
```

**Linux/Mac:**
```bash
# 验证 SHA256 校验和
sha256sum -c 镜像文件名.img.sha
```

### 3. 准备工具

根据您的操作系统选择相应的刷入工具：

- **Windows**: balenaEtcher、Rufus、USB Burning Tool
- **Linux**: `dd` 命令、balenaEtcher
- **macOS**: `dd` 命令、balenaEtcher

## 🔧 刷入方法

### 方法一：使用 TF/SD 卡刷入（推荐）

这是最简单且安全的方法，不会覆盖原系统，可以随时切换回原系统。

#### Windows 用户

**使用 balenaEtcher（推荐）**

1. 下载并安装 [balenaEtcher](https://www.balena.io/etcher/)
2. 准备一张至少 8GB 的 TF 卡或 SD 卡（建议使用 Class 10 或更高速度）
3. 使用读卡器将 TF/SD 卡连接到电脑
4. 打开 balenaEtcher
5. 点击 "Flash from file"，选择下载的 `.img` 镜像文件
6. 点击 "Select target"，选择您的 TF/SD 卡
7. 点击 "Flash!" 开始刷入
8. 等待刷入完成（通常需要 5-15 分钟）

**使用 Rufus**

1. 下载并安装 [Rufus](httpshttps://rufus.ie/)
2. 插入 TF/SD 卡
3. 打开 Rufus
4. 设备：选择您的 TF/SD 卡
5. 引导类型选择：选择 "镜像文件"，点击 "选择" 按钮选择 `.img` 文件
6. 点击 "开始" 进行刷入

#### Linux 用户

```bash
# 1. 查看连接的存储设备
lsblk

# 2. 确认 TF/SD 卡的设备名称（例如 /dev/sdb）
# 注意：请仔细确认设备名称，避免误操作！

# 3. 卸载已挂载的分区（如果有）
sudo umount /dev/sdb*

# 4. 使用 dd 命令刷入镜像
sudo dd if=镜像文件名.img of=/dev/sdb bs=4M status=progress oflag=sync

# 5. 同步缓存
sync
```

#### macOS 用户

```bash
# 1. 查看连接的存储设备
diskutil list

# 2. 确认 TF/SD 卡的设备名称（例如 /dev/disk2）
# 注意：请仔细确认设备名称，避免误操作！

# 3. 卸载磁盘
diskutil unmountDisk /dev/disk2

# 4. 使用 dd 命令刷入镜像
sudo dd if=镜像文件名.img of=/dev/rdisk2 bs=4m status=progress

# 5. 弹出磁盘
diskutil eject /dev/disk2
```

#### 启动设备

1. 将刷好镜像的 TF/SD 卡插入玩客云设备
2. 连接网线到路由器
3. 接通电源启动设备
4. 等待 1-2 分钟让系统启动
5. 通过路由器管理界面或使用 `arp -a` 命令查找设备的 IP 地址
6. 使用 SSH 连接：
   ```bash
   ssh root@设备IP地址
   # 默认密码：1234（首次登录会要求修改密码）
   ```

### 方法二：使用 USB Burning Tool 刷入（高级）

⚠️ **警告**：此方法会直接写入 eMMC，会覆盖原系统，请谨慎操作！

⚠️ **重要提示**：ARMBIAN 生成的 `.img` 镜像文件是标准的磁盘镜像格式，**无法直接使用 Amlogic USB Burning Tool 刷入**。USB Burning Tool 需要特定格式的烧录包，而 ARMBIAN 镜像不包含这种格式。

**如果您看到"解析烧录固件失败"的错误，这是正常的，因为格式不兼容。**

#### 替代方案：通过 U 盘启动后写入 eMMC（推荐）

如果您已经可以通过 U 盘启动 ARMBIAN（说明镜像正常），可以使用以下方法将系统写入 eMMC：

1. **通过 U 盘启动 ARMBIAN**
   - 将 ARMBIAN 镜像写入 U 盘（使用方法一的方法）
   - 将 U 盘插入玩客云
   - 接通电源，从 U 盘启动系统
   - 通过 SSH 登录系统

2. **将 U 盘系统写入 eMMC**
   ```bash
   # 查看存储设备
   lsblk
   
   # 确认设备名称（通常 U 盘为 /dev/sda，eMMC 为 /dev/mmcblk1）
   # 注意：请仔细确认设备名称，避免误操作！
   
   # 方法 A：使用 armbian-install 脚本（推荐）
   sudo armbian-install
   # 按照提示选择目标设备（eMMC）和分区方案
   
   # 方法 B：手动使用 dd 命令（高级用户）
   # 假设 U 盘为 /dev/sda，eMMC 为 /dev/mmcblk1
   sudo dd if=/dev/sda of=/dev/mmcblk1 bs=4M status=progress oflag=sync
   
   # 同步缓存
   sync
   
   # 重启设备（从 eMMC 启动）
   sudo reboot
   ```

3. **验证**
   - 重启后，拔出 U 盘
   - 设备应该从 eMMC 启动 ARMBIAN 系统

#### 如果必须使用 USB Burning Tool

如果您确实需要使用 USB Burning Tool，需要将 ARMBIAN 镜像转换为 Amlogic 烧录包格式（`.burn.img`）。以下是几种转换方法：

##### 方法 1：使用现成的转换工具（推荐）

**使用 aml-image-packager 或相关工具**：

```bash
# 方法 A：使用 ophub 的 amlogic-s9xxx-armbian 工具
# 这个工具主要用于编译，但可能包含转换功能
git clone https://github.com/ophub/amlogic-s9xxx-armbian.git
cd amlogic-s9xxx-armbian
# 查看文档了解如何使用

# 方法 B：查找专门的转换工具
# 在 GitHub 搜索 "armbian amlogic converter" 或 "img to burn converter"
```

##### 方法 2：手动转换（高级用户）

Amlogic USB Burning Tool 需要的 `.burn.img` 格式与标准 `.img` 的主要区别在于：

1. **分区表格式**：需要特定的分区布局
2. **引导信息**：包含 Amlogic 特定的引导配置
3. **文件系统**：可能需要特定的文件系统格式

**基本步骤**（需要 Linux 环境和相关工具）：

```bash
# 1. 准备工具
sudo apt-get install -y \
    gdisk \
    parted \
    kpartx \
    dosfstools \
    e2fsprogs

# 2. 查看原始镜像的分区信息
sudo fdisk -l armbian-image.img

# 3. 创建新的 burn 格式镜像
# 注意：这需要了解 Amlogic 设备的分区布局
# 通常需要：
# - 引导分区（包含 u-boot）
# - 系统分区（包含 ARMBIAN 系统）
# - 可能需要额外的分区

# 4. 使用工具打包（具体命令取决于使用的工具）
# 例如使用 aml-image-packager 或类似工具
```

**注意事项**：
- 转换过程复杂，需要深入了解分区布局
- 不同 Amlogic 芯片（S805、S905 等）的分区布局可能不同
- 建议参考现成的 `.burn.img` 文件结构

##### 方法 3：使用现成的 .burn.img 镜像

最简单的方法是直接使用已经转换好的 `.burn.img` 镜像：

1. **查找现成的烧录包**：
   - 搜索 "armbian onecloud burn img" 或 "armbian ws-1608 burn"
   - 查看相关 GitHub 项目，如 `hzyitc/armbian-onecloud`
   - 文件名通常包含 `.burn.img` 或 `.burn.img.xz`

2. **下载和解压**：
   ```bash
   # 如果是 .xz 压缩文件
   xz -d armbian-onecloud.burn.img.xz
   
   # 得到 armbian-onecloud.burn.img 文件
   ```

3. **使用 USB Burning Tool 刷入**：
   - 打开 USB Burning Tool
   - 导入 `.burn.img` 文件
   - 按照正常流程刷入

##### 方法 4：修改 ARMBIAN 构建流程生成 .burn.img

如果您有 ARMBIAN 构建环境，可以修改构建配置来直接生成 `.burn.img` 格式：

```bash
# 在 ARMBIAN 构建配置中
# 查找相关的打包选项，可能需要：
# - 设置特定的分区布局
# - 使用 Amlogic 特定的打包脚本
# - 参考其他项目的构建配置
```

**相关资源**：
- [ophub/amlogic-s9xxx-armbian](https://github.com/ophub/amlogic-s9xxx-armbian) - Amlogic 设备 ARMBIAN 构建工具
- [hzyitc/armbian-onecloud](https://github.com/hzyitc/armbian-onecloud) - 玩客云 ARMBIAN 项目（可能有现成的 burn 镜像）

**建议**：
- **优先使用方法 3**：直接使用现成的 `.burn.img` 镜像（如果可用）
- **如果必须转换**：参考方法 1 使用现成的转换工具
- **最后选择**：使用方法三（通过 U 盘启动后写入 eMMC），这是最简单可靠的方法

### 方法三：通过 U 盘启动后写入 eMMC（推荐用于写入 eMMC）

这是将 ARMBIAN 写入 eMMC 的推荐方法，比 USB Burning Tool 更简单可靠。

#### 步骤 1：准备 U 盘

1. 将 ARMBIAN 镜像写入 U 盘（使用方法一的方法，如 balenaEtcher 或 dd）
2. 确保 U 盘至少 8GB 容量

#### 步骤 2：从 U 盘启动

1. 将 U 盘插入玩客云设备
2. 连接网线到路由器
3. 接通电源启动设备
4. 等待 1-2 分钟让系统从 U 盘启动
5. 通过 SSH 登录系统：
   ```bash
   ssh root@设备IP地址
   # 默认密码：1234（首次登录会要求修改密码）
   ```

#### 步骤 3：写入 eMMC

**方法 A：使用 armbian-install 脚本（推荐）**

`armbian-install` 是 ARMBIAN 官方提供的安装脚本，用于将系统从外部存储安装到内部存储（如 eMMC）。

```bash
# 首先检查脚本是否存在
which armbian-install

# 如果脚本存在，运行安装脚本
sudo armbian-install

# 按照提示操作：
# 1. 选择目标设备（选择 eMMC，通常是 /dev/mmcblk1）
# 2. 选择分区方案（建议选择默认方案）
# 3. 确认后开始安装
# 4. 等待安装完成
```

**如果脚本不存在**：
- 某些 ARMBIAN 版本可能不包含此脚本
- 或者脚本位于其他位置，尝试：`/usr/local/sbin/armbian-install` 或 `armbian-config` 中的安装选项
- 如果找不到，请使用方法 B 或 C

**注意**：`armbian-install` 脚本默认会刷写 u-boot。如果您已经刷过 u-boot 或不需要更新 u-boot，建议使用方法 B（只写入系统分区），这样可以保留现有的 u-boot。

**方法 B：只写入系统分区，不刷 u-boot（推荐，如果已刷过 u-boot）**

如果您已经刷过 u-boot 或使用原厂 u-boot，只需要写入系统分区，可以使用以下方法：

```bash
# 1. 查看存储设备和分区
lsblk

# 2. 确认设备名称
#    - U 盘通常是 /dev/sda，系统分区可能是 /dev/sda2
#    - eMMC 通常是 /dev/mmcblk1，系统分区可能是 /dev/mmcblk1p2
# 注意：请仔细确认设备名称和分区号，避免误操作！

# 3. 创建挂载点
sudo mkdir -p /mnt/usb /mnt/emmc

# 4. 挂载 U 盘的系统分区（通常是第二个分区）
sudo mount /dev/sda2 /mnt/usb

# 5. 挂载 eMMC 的系统分区
sudo mount /dev/mmcblk1p2 /mnt/emmc

# 6. 使用 rsync 同步文件系统（保留 u-boot 不变）
sudo rsync -aAXv --exclude={"/dev/*","/proc/*","/sys/*","/tmp/*","/run/*","/mnt/*","/media/*","/lost+found"} /mnt/usb/ /mnt/emmc/

# 7. 同步缓存
sync

# 8. 卸载分区
sudo umount /mnt/usb /mnt/emmc

# 9. 重启设备（从 eMMC 启动）
sudo reboot
```

**方法 C：完整镜像写入（会覆盖 u-boot）**

如果您需要完整写入整个镜像（包括 u-boot），可以使用 dd 命令：

```bash
# 1. 查看存储设备
lsblk

# 2. 确认设备名称
#    - U 盘通常是 /dev/sda 或 /dev/sdb
#    - eMMC 通常是 /dev/mmcblk1
# 注意：请仔细确认设备名称，避免误操作！

# 3. 卸载 U 盘分区（如果有挂载）
sudo umount /dev/sda*

# 4. 将 U 盘镜像完整写入 eMMC（包括 u-boot）
# 假设 U 盘为 /dev/sda，eMMC 为 /dev/mmcblk1
sudo dd if=/dev/sda of=/dev/mmcblk1 bs=4M status=progress oflag=sync

# 5. 同步缓存
sync

# 6. 重启设备（从 eMMC 启动）
sudo reboot
```

**注意事项**：
- **方法 B**：只写入系统分区，保留现有的 u-boot（推荐，如果已刷过 u-boot）
- **方法 C**：完整写入，会覆盖 u-boot（如果 u-boot 有问题或需要更新）
- 使用前请确认分区号，不同设备可能不同
- 可以使用 `fdisk -l /dev/sda` 和 `fdisk -l /dev/mmcblk1` 查看详细分区信息

#### 步骤 4：验证

1. 重启后，**拔出 U 盘**
2. 设备应该从 eMMC 启动 ARMBIAN 系统
3. 通过 SSH 登录验证系统正常运行

**注意**：
- 此方法会覆盖 eMMC 中的原系统
- 确保电源稳定，避免断电
- 写入过程可能需要 10-30 分钟，请耐心等待

## 🔐 首次登录配置

### 1. SSH 登录

```bash
ssh root@设备IP地址
# 默认密码：1234
```

### 2. 修改 root 密码

首次登录会要求：
- 设置新的 root 密码
- 创建新用户（可选）

### 3. 系统更新

```bash
# 更新软件包列表
apt update

# 升级系统
apt upgrade -y

# 安装常用工具（可选）
apt install -y vim curl wget git
```

### 4. 调整显示分辨率（如果显示器超出范围）

#### 为什么需要手动设置分辨率？

**现代 Linux 桌面系统的自适应分辨率原理：**
- 通过 **EDID (Extended Display Identification Data)** 协议
- 显示器通过 HDMI/DP 接口发送自己的能力信息（支持的分辨率、刷新率等）
- 系统读取 EDID 信息，自动选择最合适的分辨率
- 这需要完整的显卡驱动和显示子系统支持

**为什么 ARMBIAN/嵌入式系统可能不支持自适应：**
1. **硬件限制**：某些 SoC（如 Amlogic S805）的显示控制器可能不支持 EDID 读取
2. **驱动支持**：嵌入式系统的显示驱动可能不完整，无法正确处理 EDID 数据
3. **U-Boot 阶段**：在 U-Boot 启动阶段就需要设置分辨率，此时还没有完整的驱动加载
4. **设备树配置**：为了兼容性和稳定性，可能需要手动配置显示参数

如果刷入后显示器提示"超出范围"或显示异常，可以通过以下方法调整 HDMI 输出分辨率：

#### 方法一：使用 armbian-config（推荐）

```bash
# 启动配置工具
sudo armbian-config

# 选择 System → Display
# 根据提示选择适合的分辨率（如 1920x1080@60Hz）
# 保存并退出，然后重启
sudo reboot
```

#### 方法二：手动修改配置文件

```bash
# 编辑启动配置文件
sudo nano /boot/armbianEnv.txt

# 添加或修改以下行（根据您的显示器调整）
# 常用分辨率示例：
# 1920x1080@60Hz
disp.screen0_output_mode=1920x1080p60

# 1280x720@60Hz
# disp.screen0_output_mode=1280x720p60

# 1024x768@60Hz
# disp.screen0_output_mode=1024x768p60

# 保存文件（Ctrl+O, Enter, Ctrl+X）
# 重启系统
sudo reboot
```

#### 方法三：通过 U-Boot 环境变量（高级）

如果上述方法无效，可以尝试通过 U-Boot 设置：

```bash
# 查看当前 U-Boot 环境变量
fw_printenv

# 设置 HDMI 分辨率
fw_setenv hdmimode 1920x1080p60hz

# 或设置其他分辨率
# fw_setenv hdmimode 1280x720p60hz
# fw_setenv hdmimode 1024x768p60hz

# 重启
sudo reboot
```

#### 常用分辨率列表

- `1920x1080p60` - Full HD 1080p @ 60Hz（最常用）
- `1280x720p60` - HD 720p @ 60Hz
- `1024x768p60` - XGA @ 60Hz
- `800x600p60` - SVGA @ 60Hz
- `640x480p60` - VGA @ 60Hz

**注意**：请根据您的显示器支持的分辨率进行选择。如果不确定，可以尝试从较低分辨率开始。

#### 方法四：尝试启用 EDID 自动检测（实验性）

某些情况下，您可以尝试让系统自动检测分辨率。在 `/boot/armbianEnv.txt` 中，可以尝试移除或注释掉 `disp.screen0_output_mode` 参数：

```bash
sudo nano /boot/armbianEnv.txt

# 注释掉或删除这一行：
# disp.screen0_output_mode=1920x1080p60

# 保存并重启
sudo reboot
```

**注意**：这不一定有效，取决于您的硬件和驱动支持。如果系统无法自动检测，显示器可能会黑屏或显示异常，此时需要通过 SSH 登录恢复设置。

## ⚠️ 注意事项

1. **备份数据**：刷入前请备份重要数据，刷入过程会清除所有数据
2. **电源稳定**：刷入过程中请确保电源稳定，避免断电
3. **设备识别**：使用 USB 刷入时，确保设备正确进入烧录模式
4. **镜像验证**：刷入前建议验证镜像文件的完整性
5. **TF/SD 卡速度**：使用 TF/SD 卡时，建议使用 Class 10 或更高速度的卡
6. **首次启动**：首次启动可能需要 1-3 分钟，请耐心等待

## 🐛 常见问题

### Q1: 刷入后无法启动？

- 检查 TF/SD 卡是否完全插入
- 确认镜像文件是否完整（重新下载并验证）
- 尝试使用不同的 TF/SD 卡
- 检查电源是否稳定

### Q2: 找不到设备 IP 地址？

```bash
# Linux/Mac 查找设备
arp -a | grep -i "armbian"

# 或使用 nmap 扫描
nmap -sn 192.168.1.0/24

# Windows PowerShell
arp -a | findstr "armbian"
```

### Q3: SSH 连接被拒绝？

- 确认设备已完全启动（等待 2-3 分钟）
- 检查网络连接
- 确认 SSH 服务已启动：`systemctl status ssh`

### Q4: 忘记 root 密码？

如果使用 TF/SD 卡启动，可以：
1. 将 TF/SD 卡插入电脑
2. 挂载根分区
3. 编辑 `/etc/shadow` 文件重置密码
4. 或重新刷入镜像

### Q5: 如何切换回原系统？

如果使用 TF/SD 卡刷入：
- 只需拔出 TF/SD 卡，设备会从 eMMC 启动原系统

如果刷入了 eMMC：
- 需要重新刷入原厂固件

### Q6: USB Burning Tool 提示"解析烧录固件失败"？

**原因**：ARMBIAN 生成的 `.img` 文件是标准磁盘镜像格式，不是 Amlogic USB Burning Tool 需要的烧录包格式（`.burn.img`）。

**解决方案**：
1. **推荐方法**：使用 U 盘启动 ARMBIAN，然后通过 `armbian-install` 或 `dd` 命令写入 eMMC（见方法三）
2. **如果必须使用 USB Burning Tool**：
   - **最简单**：使用现成的 `.burn.img` 镜像（见方法二的"方法 3"）
   - **转换镜像**：将 `.img` 转换为 `.burn.img` 格式（见方法二的详细说明）

**为什么 U 盘可以启动但 USB Burning Tool 无法刷入？**
- U 盘启动使用的是标准的磁盘镜像格式，ARMBIAN 镜像完全兼容
- USB Burning Tool 需要特定格式的烧录包（`.burn.img`），包含：
  - Amlogic 特定的分区表布局
  - 特定的引导信息结构
  - 可能需要的额外元数据
- 两种格式不兼容，所以会出现"解析失败"的错误

**如何转换镜像格式？**
详细说明请参考"方法二：使用 USB Burning Tool 刷入"章节中的转换方法。

### Q8: 找不到 armbian-install 脚本？

**检查脚本是否存在**：
```bash
# 方法 1：检查命令路径
which armbian-install

# 方法 2：检查常见位置
ls -la /usr/local/sbin/armbian-install
ls -la /usr/sbin/armbian-install

# 方法 3：搜索脚本
find /usr -name "armbian-install" 2>/dev/null
```

**如果找不到脚本**：
1. **使用 armbian-config**：某些版本可能将安装功能集成在 `armbian-config` 中
   ```bash
   sudo armbian-config
   # 查找 "Install" 或 "System" → "Install to eMMC" 选项
   ```

2. **只写入系统分区**：如果已刷过 u-boot，使用方法 B（rsync 同步文件系统）

3. **完整镜像写入**：使用方法 C（dd 命令完整写入）

4. **检查 ARMBIAN 版本**：某些较旧的版本可能不包含此脚本，建议使用最新的 ARMBIAN 镜像

### Q9: 如何只写入系统分区，不刷 u-boot？

如果您已经刷过 u-boot 或使用原厂 u-boot，只需要更新系统分区：

**推荐方法：使用 rsync 同步文件系统**

```bash
# 1. 查看分区信息
sudo fdisk -l /dev/sda    # U 盘
sudo fdisk -l /dev/mmcblk1  # eMMC

# 2. 挂载分区（假设系统分区是第二个分区）
sudo mkdir -p /mnt/usb /mnt/emmc
sudo mount /dev/sda2 /mnt/usb
sudo mount /dev/mmcblk1p2 /mnt/emmc

# 3. 同步文件系统（排除系统目录）
sudo rsync -aAXv --exclude={"/dev/*","/proc/*","/sys/*","/tmp/*","/run/*","/mnt/*","/media/*","/lost+found","/boot/*"} /mnt/usb/ /mnt/emmc/

# 4. 如果需要更新 boot 分区（但不包括 u-boot）
# 先挂载 boot 分区
sudo mount /dev/sda1 /mnt/usb/boot
sudo mount /dev/mmcblk1p1 /mnt/emmc/boot
# 只同步内核和设备树，不覆盖 u-boot
sudo rsync -aAXv --exclude="u-boot*" /mnt/usb/boot/ /mnt/emmc/boot/

# 5. 同步并卸载
sync
sudo umount /mnt/usb /mnt/emmc

# 6. 重启
sudo reboot
```

**注意事项**：
- 此方法保留现有的 u-boot
- 只更新系统文件和内核
- 需要确认正确的分区号（使用 `fdisk -l` 查看）

### Q7: 显示器提示"超出范围"怎么办？

这是 HDMI 输出分辨率与显示器不匹配导致的，解决方法：

1. **通过 SSH 登录设备**（显示器无法使用时）
2. **使用 armbian-config 调整**：
   ```bash
   sudo armbian-config
   # 选择 System → Display，选择合适的分辨率
   ```
3. **或手动修改配置文件**：
   ```bash
   sudo nano /boot/armbianEnv.txt
   # 添加：disp.screen0_output_mode=1920x1080p60
   # 保存后重启
   sudo reboot
   ```
4. **常用分辨率**：1920x1080p60、1280x720p60、1024x768p60

详细说明请参考上面的"调整显示分辨率"章节。

## 📚 相关资源

- [ARMBIAN 官方文档](https://docs.armbian.com/)
- [ARMBIAN 论坛](https://forum.armbian.com/)
- [玩客云拆机教程](https://www.znds.com/tv-1020120-1-1.html)（搜索相关教程）

## 🔗 相关链接

- [项目仓库](https://github.com/jackadam1981/WKY-armbian-build)
- [ARMBIAN 官方仓库](https://github.com/armbian/build)

---

**提示**：如有问题，请在 GitHub Issues 中反馈。

